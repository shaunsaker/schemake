import app from './app';
import firebase from './firebase';
import routes from './routes';
import SEO from './SEO';
import snackbar from './snackbar';
import types from './types';
import version from './version';

export { app, firebase, routes, SEO, snackbar, types, version };

export default {
  app,
  firebase,
  routes,
  SEO,
  snackbar,
  types,
  version,
};
