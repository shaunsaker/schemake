import css from 'styled-jsx/css'; // eslint-disable-line

const styles = css`
  .container {
    height: 100%;
    visibility: hidden;
  }

  .visible {
    visibility: visible;
  }
`;

export default styles;
